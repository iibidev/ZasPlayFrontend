export interface User{
    _id: string,
    username: string,
    profilePic: string,
    is_friend: boolean,
    createdAt: string,
    updatedAt: string,
    host: boolean,
    active: boolean,
    dev: boolean
}